class Player{
  
  PVector position,
          velocity;
  
  Player(float x, float y, float vel1, float vel2){
    position = new PVector(x, y);
    velocity = new PVector(vel1, vel2);
  }
  
}
